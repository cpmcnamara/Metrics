models:
  - name: metricflow_time_spine
    description: "MetricFlow required time spine table with daily granularity"
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
